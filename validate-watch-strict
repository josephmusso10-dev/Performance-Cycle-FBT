#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 -u "${SCRIPT_DIR}/watch_validate_recommendations.py" --strict-compatibility --compatibility-proofs "${SCRIPT_DIR}/compatibility_proofs.csv" "$@"
